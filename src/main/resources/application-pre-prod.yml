server:
  port: 8082

#  Here will be corresponding db conn for the env for our case to be simple I used one db for all
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/training_epam
    username: postgres
    password: 123
    driver-class-name: org.postgresql.Driver

logging:
  config: classpath:log-config/log4j2-pre-prod.xml